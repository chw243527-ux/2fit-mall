# Archived workflows

These legacy workflows are intentionally kept outside `.github/workflows` so GitHub Actions does not execute them. The supported paths are `flutter-ci.yml` for validation and `release-deploy.yml` for tagged releases.
